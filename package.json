{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@9.15.4",
  "license": "MIT",
  "scripts": {
    "render-build": "npx pnpm@9 install --no-frozen-lockfile && npx pnpm@9 --filter @workspace/betpulse build && npx pnpm@9 --filter @workspace/api-server build",
    "preinstall": "node -e \"console.log('Skipping preinstall on Windows')\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}